name: build process
run-name: Build process

on:
  workflow_dispatch:
  push:
  pull_request: {}


jobs:
  build-alpine:
    uses: ./.github/workflows/reusable-packer-build-diskimage.yml
    with:
      target: alpine

